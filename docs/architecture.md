# Architecture Overview

- Controller layer: auth and receipt endpoints
- Service layer: authentication, JWT, receipt handling
- Repository layer: Spring Data JPA repositories
- Frontend: static HTML/CSS/JS with Bootstrap
- Database: H2 for local development

This structure is ready for expansion with OCR, Gemini AI, analytics, reminders, and export modules.
